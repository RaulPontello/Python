## Statistics ##
# Objective: given a list of numbers, determine mean, mode, stardard deviation,...
# Python's built in functions cannot be used to determine statistics

# 1) Input

initialList = [6392, 51608, 71247, 14271, 48327, 50618, 67435, 47029, 61857, 22987, 64858, 99745, 75504, 85464, 60482, 30320, 11342, 48808, 66882, 40522]

# 2) Output

# 2.1) Initial data

sumOfAllElements = 0
sumOfAllElementsInverted = 0
productOfAllElements = 1
countOfElements = []

for i in range(0, len(initialList)):
    sumOfAllElements += initialList[i] # used in Arithimetic mean
    sumOfAllElementsInverted += (1 / initialList[i]) # used in Harmonic mean
    productOfAllElements *= initialList[i] # used in Geometric mean
    countOfElements.append(initialList.count(initialList[i])) # used in mode

countOfAllElements = dict(zip(initialList, countOfElements))
max_value = max(countOfAllElements.values())
max_keys = [k for k, v in countOfAllElements.items() if v == max_value]

# 2.2) Arithimetic mean

arithimeticMean = sumOfAllElements / len(initialList)

# 2.3) Harmonic mean

harmonicMean = len(initialList) / sumOfAllElementsInverted

# 2.4) Geometric mean

geometricMean = pow(productOfAllElements, 1 / len(initialList))

# 2.5) Mode

# see item 3

# 2.6) Median

median = 0

if len(initialList) % 2 != 0:
    median = sorted(initialList)[len(initialList) // 2]

elif len(initialList) % 2 == 0:
    initialList_inf = sorted(initialList)[0 : len(initialList) // 2]
    initialList_sup = sorted(initialList)[len(initialList) // 2 : len(initialList)]
    median = 0.5 * (initialList_inf[len(initialList_inf) - 1] + initialList_sup[0])

# 2.7) Variance

averageAbsoluteDeviation = 0

for num in initialList:
    averageAbsoluteDeviation += pow((num - arithimeticMean), 2)

populationVariance = averageAbsoluteDeviation / len(initialList)
sampleVariance = averageAbsoluteDeviation / (len(initialList) - 1)

# 2.8) Standard deviation

populationStardardDeviation = pow(populationVariance, 0.5)
sampleStardardDeviation = pow(sampleVariance, 0.5)

# 2.9) Coefficient of variation

coefficientOfVariation = populationVariance / arithimeticMean

# 3) Results

print('Arithimetic mean: ', round(arithimeticMean, 3))
print('Harmonic mean: ', round(harmonicMean, 3))
print('Geometric mean: ', round(geometricMean, 3))
print(f'Mode: {max_keys} - {max_value}')
print('Median: ', median)
print('Population variance: ', round(populationVariance, 3))
print('Sample variance: ', round(sampleVariance, 3))
print('Population standard deviation: ', round(populationStardardDeviation, 3))
print('Sample standard deviation: ', round(sampleStardardDeviation, 3))
print('Coefficient of variation: ', round(coefficientOfVariation, 3))
